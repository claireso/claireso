---
import { ViewTransitions } from "astro:transitions";
import Container from "@components/generic/Container.astro";

interface Props {
  title?: string;
}

const { title = "Front End Engineer" } = Astro.props;
---

<!doctype html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="description" content="" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <title>{`Claire Sosset ${title ? "- " + title : ""}`}</title>
    <ViewTransitions />
  </head>
  <body class="antialiased selection:bg-yellow-100">
    <header>
      <Container>
        <h1 class="text-2xl font-semibold">
          Claire Sosset <span class="hidden">-</span>
          <span class="block text-lg font-normal">Software Engineer</span>
        </h1>
      </Container>
    </header>
    <main>
      <slot />
    </main>
    <footer>
      <Container>
        <p class="text-sm border-t py-4">
          © 2024 · Fait avec 🎔 avec Astro et Tailwind CSS
        </p>
      </Container>
    </footer>
  </body>
</html>
